#!/usr/bin/python3
def print_last_digit(number):
    if number != 0:
        lastdg = str(number)[-1]
        lastdg = int(num*int(lastdg))
        
    else:
        lastdg = 0

    print (lastdg)


